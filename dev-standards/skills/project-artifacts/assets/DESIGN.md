# DESIGN

<!--
The project's design ledger. Read before any UI work; append after. Keep this comment block.
- Tokens are resolved values, not intentions. "warm neutral" is a direction;
  "#F5F1EB" is a token.
- Never delete a rejected direction — it is the record of why the project doesn't
  go that way, and deleting it invites re-litigating the same look next month.
- This file owns the record only. What the values should BE comes from the design
  skills; when a decision here conflicts with new work, the conflict gets surfaced,
  not silently overwritten.
-->

## Resolved tokens

| Token | Value | Decided |
|---|---|---|
| `--color-...` | <value> | YYYY-MM-DD |
| `--font-...` | <value> | YYYY-MM-DD |
| `--space-...` / `--radius-...` | <value> | YYYY-MM-DD |

## Component inventory

| Component | Status | Location | Notes |
|---|---|---|---|
| <name> | built / mocked / planned | <path> | <variants, states covered> |

## Directions tried and rejected

- <direction, named concretely enough to recognize it when it comes back> — <the specific reason it was rejected>

## Open questions

- <unresolved visual decision> — <what's blocking it>
