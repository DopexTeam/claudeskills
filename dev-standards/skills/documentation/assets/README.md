# <Project name>

<!--
Developer-facing. The reader is a developer in their first ten minutes with the repo.
- Every command below must have been actually run against this repo before it's written
  here. A README quickstart that fails on step 2 teaches distrust of every other doc.
- Describe the project as it IS. Future work goes under Roadmap, in future tense,
  or nowhere.
- Keep the "verify it worked" step — setup without a success check strands the reader
  in an unknown state.
-->

<One honest paragraph: what this is, who uses it, and what it talks to. No adjectives
the code can't back up.>

**Status:** <production / used internally / prototype — as of YYYY-MM-DD>

## Quickstart

Prerequisites: <runtime + version, tools, access — everything a fresh machine lacks>

```bash
<clone / install / configure — exact commands, in order>
<run>
```

**Verify it worked:** <the one command or URL and what it should show>

## Configuration

| Variable | Required | What it does | Example / where to get it |
|---|---|---|---|
| `<VAR>` | yes/no | <effect> | <source> |

## Project structure

<Five lines max — the directories a newcomer actually needs, annotated. Point at
ARCHITECTURE.md for the real map.>

## Development

- Run tests: `<command>`
- <other common tasks: lint, local data, etc.>

## Deployment

<One line + link to the runbook (RUNBOOK-deploy.md) — do not restate procedures here;
two copies of a deploy procedure will disagree eventually.>

## Further reading

- [ARCHITECTURE.md](./ARCHITECTURE.md) — how it works and why
- <DECISIONS.md, API docs, user docs — whatever exists>
