# <Product name> — User guide

<!--
END-USER-facing. The reader uses the software; they did not build it and never will.
- Reader's vocabulary only: no code, no schema, no "endpoint", "database", "null",
  "sync job". If the UI calls it "Submit", this document says Submit — the exact
  on-screen word, always.
- Task-oriented: users arrive asking "how do I X", not "what is the invoice module".
- Every claim verified against the running product before it's written. A manual that
  describes a button that isn't there costs the reader's trust in every page.
- Status names, menu labels, and messages are transcribed from the real screens,
  never recalled.
-->

## What <Product> is

<Two or three sentences, in the user's terms: what it's for, what they can do with it.
Follow with one honest line about what it does NOT do, if users will predictably
expect it ("<Product> shows payment status; payments themselves are made by <who>").>

## Getting in

- **Where:** <URL / app>
- **Signing in:** <how, from the user's point of view>
- **No account or locked out?** <who to contact, exactly>

## Finding your way around

<One short paragraph orienting the main screen: what's where, using the exact labels
on screen. A user should be able to hold this in their head.>

## How do I…

<One subsection per task, in the order users are likely to need them. Numbered steps;
each step is one action; every label in quotes is the exact on-screen text.>

### …<do the most common task>?

1. <action>
2. <action>
3. **What you should see:** <the confirmation state — what tells the user it worked>

**If that didn't happen:** <the likely cause and what to do, in user terms>

## What the statuses mean

| Status | What it means | What happens next | What you should do |
|---|---|---|---|
| `<exact status label>` | | | <often "nothing — this is normal"> |

## Common questions

**<Question users actually ask>**
<Answer. If the honest answer is "you can't do that yet," say exactly that.>

## Getting help

<Who to contact, how, and what to include so the first reply is useful (what you were
doing, what you saw, when).>
